name: Diagnóstico — quem responde no motor de IA

# SÓ à mão. Não tem horário, não escreve no repositório, não publica nada:
# faz uma pergunta minúscula ao motor de texto e diz qual fornecedor respondeu.
#
# Existe porque em 18/08/2026 a Cerebras começou a recusar (HTTP 402) e a única
# maneira de saber se tinha voltado era esperar que um robô de CONTEÚDO corresse
# por acaso — e esses publicam. Medir não pode custar uma publicação.
on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  diagnosticar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Perguntar ao motor de texto e à fila normal de imagens
        env:
          CEREBRAS_API_KEY: ${{ secrets.CEREBRAS_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          KIE_API_KEY: ${{ secrets.KIE_API_KEY }}
          NVIDIA_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_AI_TOKEN: ${{ secrets.CLOUDFLARE_AI_TOKEN }}
          TOGETHER_API_KEY: ${{ secrets.TOGETHER_API_KEY }}
        run: node src/scripts/diagnostico/fornecedores-ia.js

      # O GEMINI SOZINHO. Sem isto ele nunca seria experimentado: está atrás do
      # Groq na fila de propósito (só acorda quando o Groq estoura os 200 mil do
      # dia), e o roteador pára no primeiro que responde. Aqui as outras chaves
      # ficam de fora e, como cada arranque monta a lista de novo, ele passa a
      # ser o primeiro.
      # `continue-on-error`: o Gemini é SOCORRO — se estiver em baixo, o
      # diagnóstico tem de DIZER isso, não morrer e esconder o resto.
      - name: O Gemini sozinho (o socorro dos dias de pico)
        continue-on-error: true
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: node src/scripts/diagnostico/fornecedores-ia.js --so-texto

      # A NVIDIA SOZINHA, pela mesma razão do passo acima.
      # Enquanto não houver `NVIDIA_API_KEY`, este passo diz "nenhum fornecedor
      # configurado" e segue — que é a resposta HONESTA, e não um verde falso.
      - name: A NVIDIA sozinha (o substituto da Cerebras)
        continue-on-error: true
        env:
          NVIDIA_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
        run: node src/scripts/diagnostico/fornecedores-ia.js --so-texto

      # A Together SOZINHA. Sem isto ela nunca seria experimentada: o roteador
      # para no primeiro que responder, e a Cloudflare responde primeiro.
      # Aqui as chaves da Cloudflare são deixadas de fora de propósito, e como
      # este é um arranque novo a lista de fornecedores é montada outra vez.
      # `continue-on-error`: a Together é o PLANO B — se ela estiver em baixo o
      # diagnóstico deve DIZER isso, não morrer e esconder o resto.
      # O GROQ_API_KEY vai aqui de propósito, embora este passo seja sobre a
      # Together: é ele que dá olhos à trava anti-letras. Sem ele a imagem da
      # Together sairia sem ninguém a olhar — e é justamente a dela que mais
      # interessa ver, porque o Qwen-Image é um modelo ESPECIALISTA em escrever
      # texto (foi o substituto imposto quando a Together desligou o FLUX).
      - name: A Together sozinha (o plano B das imagens)
        continue-on-error: true
        env:
          TOGETHER_API_KEY: ${{ secrets.TOGETHER_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: node src/scripts/diagnostico/fornecedores-ia.js --so-imagem
      # O SALDO DA CONTA QUE PAGA OS ROTEIROS.
      # Existe porque em 18/09/2026 o saldo chegou a zero e ninguém soube
      # durante 9 dias: o kie.ai não dá erro quando não há crédito — devolve
      # "resposta vazia", a fila cai nos gratuitos, e a corrida segue. Só se viu
      # pela qualidade a cair (reprovações do roteiro de 0% para 93%).
      # `continue-on-error`: saber o saldo é informação, não é condição de vida.
      - name: Quanto resta na conta paga (kie.ai)
        continue-on-error: true
        env:
          KIE_AI_KEY: ${{ secrets.KIE_AI_KEY }}
        run: node src/scripts/diagnostico/saldo-kie.js
