---
import { getCollection } from 'astro:content';

// Categorias de artigo com posts (evita link para página 404).
const CAT_LABELS: Record<string, string> = {
  dicas: 'Dicas', orcamento: 'Orçamento', investimentos: 'Investimentos', cotacoes: 'Cotações',
};
const posts = await getCollection('posts', ({ data }) => !data.draft && data.locale === 'pt');
const catSet = new Set(posts.map(p => p.data.category));
const categorias = Object.entries(CAT_LABELS)
  .filter(([slug]) => catSet.has(slug))
  .map(([slug, label]) => ({ slug, label, href: `/categorias/${slug}` }));

const navItems = [
  { label: 'Ferramentas', href: '/ferramentas', i18n: 'nav.ferramentas' },
  { label: 'Estudos', href: '/estudos', i18n: 'nav.estudos' },
  { label: 'Glossário', href: '/glossario', i18n: 'nav.glossario' },
  { label: 'Sobre', href: '/sobre', i18n: 'nav.sobre' },
  { label: 'Baixar App', href: '/app', cta: true, i18n: 'nav.baixarApp' },
];
---

<button class="mobile-toggle" aria-label="Abrir menu" aria-expanded="false" id="mobile-menu-toggle">
  <span class="hamburger"></span>
</button>

<div class="mobile-overlay" id="mobile-overlay"></div>
<div class="mobile-menu" id="mobile-menu" aria-hidden="true">
  <nav class="mobile-nav" aria-label="Menu mobile">
    <ul class="mobile-nav-list">
      <li>
        <a href="/" class="mobile-nav-link" data-i18n="nav.inicio">Início</a>
      </li>
      <li class="mobile-cat-label" data-i18n="nav.categorias">Categorias</li>
      {categorias.map(c => (
        <li>
          <a href={c.href} class="mobile-nav-link mobile-sublink">{c.label}</a>
        </li>
      ))}
      {navItems.map(item => (
        <li>
          <a href={item.href} class:list={['mobile-nav-link', { cta: item.cta }]} data-i18n={item.i18n}>
            {item.label}
          </a>
        </li>
      ))}
    </ul>
  </nav>
</div>

<script>
  const toggle = document.getElementById('mobile-menu-toggle');
  const menu = document.getElementById('mobile-menu');
  const overlay = document.getElementById('mobile-overlay');

  // Move overlay and menu to body to escape header's stacking context
  if (overlay) document.body.appendChild(overlay);
  if (menu) document.body.appendChild(menu);

  function openMenu() {
    toggle?.setAttribute('aria-expanded', 'true');
    menu?.setAttribute('aria-hidden', 'false');
    overlay?.classList.add('active');
    document.body.style.overflow = 'hidden';
  }

  function closeMenu() {
    toggle?.setAttribute('aria-expanded', 'false');
    menu?.setAttribute('aria-hidden', 'true');
    overlay?.classList.remove('active');
    document.body.style.overflow = '';
  }

  toggle?.addEventListener('click', () => {
    const isOpen = toggle.getAttribute('aria-expanded') === 'true';
    if (isOpen) closeMenu();
    else openMenu();
  });

  overlay?.addEventListener('click', closeMenu);

  // Close on link click
  menu?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', closeMenu);
  });
</script>

<style>
  .mobile-toggle {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: var(--radius-md);
    transition: background var(--transition-fast);
  }

  .mobile-toggle:hover {
    background: var(--bg-tertiary);
  }

  @media (min-width: 768px) {
    .mobile-toggle {
      display: none;
    }
  }

  .hamburger {
    position: relative;
    width: 20px;
    height: 2px;
    background: var(--text-primary);
    border-radius: 2px;
    transition: all var(--transition-fast);
  }

  .hamburger::before,
  .hamburger::after {
    content: '';
    position: absolute;
    left: 0;
    width: 100%;
    height: 2px;
    background: var(--text-primary);
    border-radius: 2px;
    transition: all var(--transition-fast);
  }

  .hamburger::before { top: -6px; }
  .hamburger::after { top: 6px; }

  [aria-expanded="true"] .hamburger {
    background: transparent;
  }

  [aria-expanded="true"] .hamburger::before {
    top: 0;
    transform: rotate(45deg);
  }

  [aria-expanded="true"] .hamburger::after {
    top: 0;
    transform: rotate(-45deg);
  }

  .mobile-overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.5);
    z-index: 299;
  }

  .mobile-overlay.active {
    display: block;
  }

  .mobile-menu {
    position: fixed;
    top: 0;
    right: 0;
    bottom: 0;
    width: 80%;
    max-width: 320px;
    background-color: #0d1117;
    padding: var(--space-8);
    padding-top: 100px;
    transform: translateX(100%);
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: var(--z-modal);
    box-shadow: -4px 0 20px rgba(0, 0, 0, 0.3);
    overflow-y: auto;
  }

  :root[data-theme="light"] .mobile-menu {
    background-color: #ffffff;
    box-shadow: -4px 0 20px rgba(0, 0, 0, 0.1);
  }

  .mobile-menu[aria-hidden="false"] {
    transform: translateX(0);
  }

  @media (min-width: 768px) {
    .mobile-menu {
      display: none;
    }
  }

  .mobile-nav-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .mobile-nav-link {
    display: block;
    padding: var(--space-4);
    color: var(--text-primary);
    font-size: var(--text-lg);
    font-weight: 500;
    border-radius: var(--radius-md);
    transition: background var(--transition-fast);
  }

  .mobile-nav-link:hover {
    background: var(--bg-secondary);
  }

  .mobile-nav-link.cta {
    margin-top: var(--space-4);
    background: var(--gradient-cta);
    color: #fff;
    text-align: center;
    font-weight: 600;
    border-radius: var(--radius-full);
  }

  .mobile-cat-label {
    padding: var(--space-3) var(--space-4) var(--space-1);
    color: var(--text-tertiary);
    font-size: var(--text-xs);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: var(--tracking-wide);
  }

  .mobile-sublink {
    padding-left: var(--space-6);
    font-size: var(--text-base);
  }
</style>
